# Prompt Images

Place writing-prompt images here.

Expected file for the Unfall topic:

```text
public/prompt-images/unfall.png
```

The generated materials CSV references it as:

```text
/prompt-images/unfall.png
```

Files inside `public/` are served by Next.js from the site root.
